<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Compatibility shim for environments/build caches still referencing Material3 XML style name. -->
    <style name="Theme.Material3.DayNight.NoActionBar" parent="Theme.MaterialComponents.DayNight.NoActionBar" />

    <style name="Theme.StarSetTracker" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <item name="android:statusBarColor" tools:targetApi="l">@android:color/transparent</item>
    </style>
</resources>
